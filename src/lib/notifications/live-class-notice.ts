/** WhatsApp class notices disabled — students use portal only. */
export async function notifyStudentsOfLiveClass(_input: {
  programSlug: string;
  title: string;
  date: string;
  time: string;
  trainerName: string;
}): Promise<void> {
  return;
}
